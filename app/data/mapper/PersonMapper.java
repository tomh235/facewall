package data.mapper;

import data.builder.PersonBuilder;
import domain.Person;
import org.neo4j.graphdb.Node;

public class PersonMapper {
    public Person map(PersonBuilder personBuilder, Node personNode) {
        return null;
    }
}
