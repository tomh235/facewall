# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page (Overview Page)
GET     /                           controllers.OverviewAndSearchController.overview

# Search page
GET     /search                     controllers.OverviewAndSearchController.search

# Search Results page
GET     /search-results             controllers.OverviewAndSearchController.searchResults

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# New userModel registration form
GET     /signupform                 controllers.SignUpController.blankSignUpForm
POST    /signupform                 controllers.SignUpController.signUpFormSubmit