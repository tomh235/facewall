# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page (Overview Page)
GET     /                           controllers.FacewallScalaController.overview

# Search page
GET     /search                     controllers.FacewallScalaController.search

# Search Results page
GET     /search-results             controllers.FacewallScalaController.searchResults

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# New user registration form
GET     /signupform                 controllers.SignupController.blankSignUpForm
POST    /signupform                 controllers.SignupController.signUpFormSubmit

# New user registration summary
#GET    /signupsummary              controllers.SignupController.signUpFormSubmit
