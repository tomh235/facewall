# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page (Overview Page)
GET     /                           controllers.FacewallController.overview

# Search page
GET     /search                     controllers.FacewallController.search

# Search Results page
GET     /search-results                     controllers.FacewallController.searchResults

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# Form
GET     /newuser                       controllers.FacewallController.newUser
POST    /newuser                       controllers.FacewallController.newUserSubmit
